# Example dashboard using ricjs

## Usage

Clone the repository and then execute the following commands from the root directory

```bash
$ npm install
$ cd examples/dashboard
$ npm install
$ npm start
```